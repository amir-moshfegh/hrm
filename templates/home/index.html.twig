{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}



    <a class="" data-toggle="collapse" href="#contentId" aria-expanded="false"
       aria-controls="contentId">
        <i class="fa fa-arrow-circle-down" aria-hidden="true"></i>
    </a>

    <div class="collapse" id="contentId">
        test
    </div>

    <table class="table">
        <thead>
        <tr>
            <th></th>
            <th></th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for history in histories %}
            <tr>

                <td> {{ history.base.category.name }}</td>
                <td> {{ history.base.name }}</td>
                <td>{{ history.noticeDate|date }}</td>

                <td>
                    {% if history.endDate==null %}
                        <i class="fa fa-check" aria-hidden="true"></i>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}

        </tbody>
    </table>



{% endblock %}